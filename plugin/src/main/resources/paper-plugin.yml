name: IslandPortal
version: '${version}'
main: net.danh.islandportal.IslandPortal
api-version: '26.1.2'
load: POSTWORLD
folia-supported: true
authors: [ VoChiDanh ]
dependencies:
  server:
    BentoBox:
      load: BEFORE
      required: false
    SuperiorSkyblock2:
      load: BEFORE
      required: false
    Skyllia:
      load: BEFORE
      required: false
    packetevents:
      load: BEFORE
      required: false
    WorldEdit:
      load: BEFORE
      required: false
    FastAsyncWorldEdit:
      load: BEFORE
      required: false
permissions:
  islandportal.admin:
    default: op
